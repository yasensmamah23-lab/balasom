<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>لوحة تحكم المستخدم</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-slate-50 p-6">
    <div class="max-w-md mx-auto">
        <h1 class="text-2xl font-bold text-slate-800 mb-8">مرحباً بك في بلسم</h1>
        
        <div class="space-y-6">
            <!-- زر الطوارئ SOS -->
            <button class="bg-red-600 text-white w-full py-6 rounded-2xl text-2xl font-bold shadow-lg hover:bg-red-700 transition">
                🚨 طلب إسعاف فوري (SOS)
            </button>

            <!-- زر طلب عادي -->
            <button class="bg-blue-600 text-white w-full py-4 rounded-xl font-bold hover:bg-blue-700 transition">
                طلب زيارة منزلية
            </button>
        </div>
    </div>
</body>
</html>
